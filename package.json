{
  "name": "@mansi-manhas/shared-ui-monorepo",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "lint:versions": "syncpack lint",
    "build": "pnpm -r run build",
    "typecheck": "pnpm -r run build && pnpm -r run typecheck",
    "dev:demo": "pnpm --filter @mansi-manhas/demo-app dev"
  },
  "devDependencies": {
    "@changesets/changelog-github": "^1.0.0",
    "@changesets/cli": "^3.0.1",
    "syncpack": "15.3.3",
    "typescript": "^5.4.2"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devEngines": {
    "packageManager": {
      "name": "pnpm",
      "version": "12.0.0",
      "onFail": "download"
    }
  },
  "packageManager": "pnpm@12.0.0",
  "type": "module",
  "publishConfig": {
    "registry": "https://npm.pkg.github.com/"
  }
}
